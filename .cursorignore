# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)

# Bazel build artifacts
/bazel-*

# Generated data and build artifacts (but keep venv/ searchable for debugging)
runs/
__pycache__/
*.pyc
build/
dist/
*.egg-info/
node_modules/

# Large binary files and logs
*.log
*.so
*.dll
*.dylib

# IDE and editor files
.idea/
.vscode/
# Note: .cursor/ directory contains rules and should be indexed
# .cursor/

# Temporary and cache files
.cache/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
