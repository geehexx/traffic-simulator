# Core dependencies - compatible versions for Bazel and development
numpy>=1.24.3
arcade>=3.3.2
pymunk>=6.5.0
pyyaml>=6.0.1
psutil>=5.9.5
numba>=0.57.1

# Testing dependencies
pytest>=8.4.2
pytest-xdist>=3.6.0
pytest-cov>=7.0.0
pytest-mock>=3.14.0
hypothesis>=6.111.0

# Note: Removed unused dependencies in recent cleanup:
# - scipy, pandas, matplotlib, seaborn, plotly (visualization tools)
# - joblib, h5py (data processing)
# - memory-profiler, line-profiler, pympler (profiling tools)
# - asv (benchmarking)
# These were never imported in source code and have been removed to reduce bloat
